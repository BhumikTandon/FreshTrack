# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'  # Update this to match your deployment target

target 'Food Expiry Tracker' do
  use_frameworks!

  # ✅ Add ParseSwift here (inside the main app target)
  pod 'ParseSwift'

  target 'Food Expiry TrackerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Food Expiry TrackerUITests' do
    # Pods for UI testing
  end

end
